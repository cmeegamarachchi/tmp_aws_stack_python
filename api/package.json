{
  "name": "aws-stack-api",
  "version": "1.0.0",
  "description": "AWS Lambda API with TypeScript",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "package": "npm run build && zip -r lambda-deployment.zip dist/ node_modules/",
    "test": "jest",
    "dev": "ts-node src/index.ts"
  },
  "dependencies": {
    "aws-lambda": "^1.0.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "@types/node": "^20.4.5",
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "jest": "^29.6.1",
    "@types/jest": "^29.5.3",
    "ts-jest": "^29.1.1"
  }
}
